name: Build iOS IPA (Using xcodegen)
on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  build-ipa:
    runs-on: macos-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 清理旧产物
        run: |
          rm -rf Habitapp.xcodeproj Habitapp build IPA Payload 2>/dev/null || true

      - name: 安装 xcodegen
        run: |
          brew install xcodegen

      - name: 生成项目配置文件 (project.yml)
        run: |
          cat > project.yml << 'EOF'
          name: Habitapp
          options:
            minimumDeploymentTarget: 15.0
          targets:
            Habitapp:
              type: application
              platform: iOS
              sources:
                - Habitapp/Classes
              settings:
                INFOPLIST_FILE: Habitapp/Resources/Info.plist
                PRODUCT_BUNDLE_IDENTIFIER: com.yourteam.Habitapp # 替换为你的真实Bundle ID
                CODE_SIGN_IDENTITY: "iPhone Distribution"
                CODE_SIGN_STYLE: Manual
                DEVELOPMENT_TEAM: 6GMTCJDKPM # 替换为你的Team ID
          EOF

      - name: 生成 Info.plist
        run: |
          mkdir -p Habitapp/Resources
          cat > Habitapp/Resources/Info.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>$(EXECUTABLE_NAME)</string>
              <key>CFBundleIdentifier</key>
              <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
              <key>CFBundleVersion</key>
              <string>1</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>MinimumOSVersion</key>
              <string>15.0</string>
              <key>UIApplicationSceneManifest</key>
              <dict>
                  <key>UIApplicationSupportsMultipleScenes</key>
                  <false/>
              </dict>
          </dict>
          </plist>
          EOF

      - name: 生成 App 入口文件
        run: |
          mkdir -p Habitapp/Classes
          cat > Habitapp/Classes/App.swift << 'EOF'
          import UIKit
          @main
          class HabitappApp: UIResponder, UIApplicationDelegate {
              var window: UIWindow?
              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  window = UIWindow(frame: UIScreen.main.bounds)
                  window?.rootViewController = UIViewController()
                  window?.rootViewController?.view.backgroundColor = .white
                  window?.makeKeyAndVisible()
                  return true
              }
          }
          EOF

      - name: 生成 Xcode 项目 (使用 xcodegen)
        run: |
          xcodegen generate

      - name: 导入签名证书
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.P12_FILE }}
          p12-password: ${{ secrets.P12_PASSWORD }}

      - name: 安装描述文件
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "${{ secrets.PROVISIONING_PROFILE }}" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/Habitapp.mobileprovision

      - name: 编译归档
        run: |
          xcodebuild archive \
            -project Habitapp.xcodeproj \
            -scheme Habitapp \
            -destination 'generic/platform=iOS' \
            -archivePath ./build/Habitapp.xcarchive \
            PROVISIONING_PROFILE_SPECIFIER=Habitapp \
            -verbose

      - name: 导出 IPA
        run: |
          cat > ExportOptions.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>ad-hoc</string>
              <key>teamID</key>
              <string>6GMTCJDKPM</string> <!-- 替换为你的Team ID -->
              <key>provisioningProfiles</key>
              <dict>
                  <key>com.yourteam.Habitapp</key> <!-- 替换为你的真实Bundle ID -->
                  <string>Habitapp</string>
              </dict>
              <key>signingCertificate</key>
              <string>iPhone Distribution</string>
              <key>signingStyle</key>
              <string>manual</string>
          </dict>
          </plist>
          EOF

          xcodebuild -exportArchive \
            -archivePath ./build/Habitapp.xcarchive \
            -exportOptionsPlist ExportOptions.plist \
            -exportPath ./IPA \
            -verbose

      - name: 上传 IPA 产物
        uses: actions/upload-artifact@v4
        with:
          name: Habitapp-IPA
          path: ./IPA/*.ipa